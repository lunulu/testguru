<tr>
  <td class="d-grid gap-2"><%= link_to "#{test.title}", admin_test_path(test), class: 'btn btn-secondary' %></td>
  <td><%= t("tests_helper.#{test_level(test)}") %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('.edit.'), edit_admin_test_path(test), class: 'btn btn-warning' %></td>
  <td><%= link_to t('.delete.'), admin_test_path(test), method: :delete, data: {confirm: t('.confirm')}, class: 'btn btn-danger' %></td>
</tr>
